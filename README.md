# solved-tasks
#### Be Concise I - The Ternary Operator
```javascript
function describeAge(age){
  let a=(age>64)?"elderly":
  (age>17)?"adult":
  (age>12)?"teenager":"kid";
   return `You're a(n) ${a}`;
};
```
