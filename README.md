# solved-tasks
#### Binary Addition
```javascript
function addBinary(a,b) {
  return (a+b).toString(2);
}
```
#### Convert to Binary
```javascript
function toBinary(n){
  return +n.toString(2);
}
```















